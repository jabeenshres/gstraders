from django.urls import path

app_name = 'dashboard'

urlpatterns = [
    
    # path('create/', AppointmentCreateView.as_view(), name='create'),
    # # path('select/<int:pk>', AppointmentUpdateView.as_view(), name='update'),
    
    # # path('info/', InformationPageView.as_view(), name='info'),
    # path('dates/', AvailableDatesView.as_view(), name='dates'),
    # path('success/', SuccessCreateView.as_view(), name='success'),
    
    
]